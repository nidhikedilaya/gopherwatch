GopherWatch Dashboard (Frontend)

This is the React/Vite frontend for the GopherWatch capstone project.

## Prerequisites
* **Node.js and npm** must be installed.
* The **GopherWatch Go Backend** must be actively running locally

Install dependencies and start the development server:
npm install
npm run dev